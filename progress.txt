================================================================================
TINDAGO — WEB APP PROGRESS
================================================================================
Date: Aug 18–20, 2026
Project: TindaGo Web App (formerly Sari-Sari Smart)
================================================================================

PROJECT OVERVIEW
--------------------------------------------------------------------------------
TindaGo is a mobile-first daily business companion for traditional sari-sari
store owners. Refactored from an item-by-item POS system to a daily awareness
tool based on interview findings. Three pillars: Stock Tracker | Daily Cash
Summary | Utang Notebook.

Originally built as "Sari-Sari Smart" (June–Aug 2026). Rebranded to "TindaGo"
on Aug 18, 2026.

================================================================================
V1.0 — REBRAND: SARISARISMART → TINDAGO (WEB) — DONE
Date: Aug 18, 2026
Status: IMPLEMENTED + VERIFIED

WHAT CHANGED (user-facing):
- All 17 HTML page <title> tags updated from "Sari-Sari Smart — …" to
  "TindaGo — …"
- Welcome text on index.html and about text on help.html updated
- Support email changed from support@sarisarismart.com to support@tindago.com

WHAT CHANGED (code):
- app.js i18n dictionaries (EN + FIL): setupTitle, mainTutorial1,
  helpTutorial1, aboutText, contactContent — all updated to TindaGo
- CSV export headers: "TindaGo — Data Export" and "TindaGo Report"
- Download filenames: tindago-data.json, tindago-data.csv,
  tindago-report-YYYY-MM-DD.csv
- Code comments in app.js, style.css, restock.js header banners

DATA/STORAGE:
- localStorage keys unchanged (sss_v3_* prefix) — backward-compatible with
  existing user data. Renaming storage keys would break all existing installs.

DEPENDENCIES/INFRASTRUCTURE:
- Git remote still points at old GitHub repo (Sari-sari_smart.git) — pending
  GitHub rename
- Vercel project name pending update
- support@tindago.com domain must exist before shipping

VERIFICATION:
- node --check app.js — clean.
- tests/i18n-check.js: all 109 data-i18n keys resolve in EN + FIL.
- tests/checkout-smoke.js: 80/80 PASS.

FILES AFFECTED (21 modified):
  add_product.html, app.js, checkout.html, closing.html, day.html,
  debtor_detail.html, debts.html, expenses.html, help.html, index.html,
  inventory.html, morning.html, new_debt.html, product_detail.html,
  record_payment.html, reports.html, restock.html, restock.js, setting.html,
  style.css

================================================================================
V1.1 — CLEANUP: REMOVE DEV ARTIFACTS (WEB) — DONE
Date: Aug 18, 2026
Status: IMPLEMENTED — dev-only files removed, tutorials retained

REMOVED (no longer needed for the app to run):
- .vscode/settings.json (IDE config, personal dev setting)
- progress.txt (old Sari-Sari Smart changelog, 4290 lines)
- analysis.txt (feature analysis doc)
- instructions.txt, instructions 2.txt, instructions 3.txt (build instructions)
- fix_tutorial.py (utility script for localStorage repair)
- tests/checkout-smoke.js, tests/i18n-check.js (test files)

KEPT:
- tutorials/ (17 files — tutorial spec docs for the overlay system)

FILES REMAINING (38 total):
  HTML pages (17): index, morning, day, closing, inventory, add_product,
    product_detail, debts, new_debt, debtor_detail, record_payment,
    expenses, restock, reports, setting, help, checkout
  JS (2): app.js, restock.js
  CSS (1): style.css
  Tutorials (17): README + 16 tutorial spec files

================================================================================
V2.0 — SAMPLE DATA OVERHAUL + UI POLISH (WEB) — DONE
Date: Aug 18, 2026
Status: IMPLEMENTED + VERIFIED

V2.0a — 120-item sample dataset
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Default inventory expanded from 17 products → 120 products
- 20 product categories (was 9): Soft Drinks, Bottled Water, Instant Coffee,
  Instant Noodles, Rice, Canned Sardines, Canned Tuna, Eggs, Bread/Pandesal,
  Biscuits/Cookies, Chocolate/Candy, Chips/Snacks, Salt, Sugar, Shampoo
  Sachets, Bath Soap, Laundry Detergent, Toothpaste/Toothbrush, Mosquito
  Coils, Cigarettes
- 5 new unit types (was 12): sack, loaf, tube, bar, sticks
- Every category has 4-6 competing brands (e.g. Canned Tuna: Century, 555,
  Mega, San Marino, Ligo, Family's Choice)
- Stock variety: 13 out-of-stock, 17 low-stock, 90 plenty
- Old dataset auto-migrated: detects old 'p1'-style IDs and replaces with
  new 's1'-style 120-item dataset on load

WHAT CHANGED (code):
- getSampleProducts(): replaced with 120-item array
- PRODUCT_CATEGORIES: expanded from 9 → 20 keys
- PRODUCT_UNITS: expanded from 12 → 17 keys (+sack, loaf, tube, bar, sticks)
- productCategoryLabel(): updated when-map for 20 categories
- productUnitLabel(): updated when-map for 17 units
- i18n: 20 new cat* keys + 5 new unit* keys in EN and FIL
- loadState(): migration detection — old 'p' prefix IDs trigger reseed

V2.0b — Category filter chips: horizontal scroll + expand
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Category chip row changed from wrapping to horizontal scroll
- Shows first 6 chips (All + 5 categories) in a single row
- "More ▾" / "Dagdag ▾" button at the end to expand all 20
- "Less ▴" / "Bawas ▴" button to collapse back
- Mouse wheel vertical scroll blocked on chip container (desktop)

WHAT CHANGED (code):
- .cat-chips: flex-wrap:nowrap + overflow-x:auto + hidden scrollbar
- .cat-chip: white-space:nowrap + flex-shrink:0
- .cat-chip-more: new style (green outline)
- renderInventoryCatFilters(): toggle between collapsed/expanded
- toggleCatFilters(): exposed to window
- Wheel event listener: preventDefault on vertical wheel intent

V2.0c — Morning stock card: top 2 items only
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Morning page stock warning card now shows only top 2 items
- Out-of-stock items shown first, then low-stock
- "+N more" / "+N pang" indicator for remaining items
- Card stays compact instead of growing with every low item

WHAT CHANGED (code):
- renderMorningCheck(): sorted lowItems (out first), sliced to top 2
- Added moreCount indicator when >2 items exist

V2.0d — Inventory back-to-top button
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Floating circle button appears at bottom-right of inventory page
- Button appears when user scrolls past the category filter chips
- Tap to smooth-scroll back to top of inventory
- Button disappears when at top

WHAT CHANGED (code):
- inventory.html: added #backToTopBtn (absolute position, bottom-right)
- style.css: .back-to-top floating button styles
- app.js: scroll event listener on #appContent — compares catFilters
  bottom against contentRect.top to toggle button visibility

FILES AFFECTED:
  app.js (sample data, cat filter, morning stock, back-to-top JS)
  style.css (cat chips, cat-chip-more, back-to-top button)
  inventory.html (back-to-top button markup)

VERIFICATION:
- node --check app.js — clean.

================================================================================
V2.1 — BUG FIX: CATEGORY CHIPS EXPAND BEHAVIOR + INVENTORY BOTTOM GAP (WEB)
Date: Aug 19, 2026
Status: N/A (web already correct)

NOTES:
- V2.0e and V2.0f below were fixes applied to the mobile app to match the
  web app. The web app already had the correct behavior:
    • Category chips use flex-wrap:wrap when expanded (wraps to multiple lines)
    • .app-content has padding-bottom:80px creating gap before bottom nav
- No changes needed on the web side.

================================================================================
V2.2 — BUG FIX: REMOVE INVENTORY BOTTOM GAP (WEB)
Date: Aug 19, 2026
Status: IMPLEMENTED

PROBLEM:
- .app-content had padding-bottom:80px creating a visible gap between the
  last .inv-manage-item and the .bottom-nav bar
- The 80px padding was a redundant artifact — the bottom nav sits as a flex
  sibling BELOW .app-content (not fixed-positioned/overlapping), so no
  scroll-protection padding is needed

SOLUTION:
- Changed .app-content padding from `16px 16px 80px` to `16px 16px 16px`

WHAT CHANGED (code):
- style.css: .app-content padding-bottom reduced from 80px to 16px

FILES AFFECTED:
  style.css

================================================================================

V2.3 — BUILD FIX: TUTORIAL AUTO-SCROLL COMPILATION ERRORS (WEB N/A)
Date: Aug 20, 2026
Status: N/A (web app not affected — changes are mobile-only)

NOTES:
- V2.0h introduced TutorialScrollStateHolder and auto-scroll restoration
  for the mobile app. The web app's tutorial auto-scroll was already
  functional and required no changes.
- Two compilation errors were fixed in the mobile app:
    1. Smart cast impossible on delegated properties (by rememberUpdatedState)
    2. JVM signature clash (var property setter vs explicit fun setter)
- Both fixes are logged in the mobile app progress (V2.0h update below).

================================================================================
V2.4 — RESTOCK BACK NAVIGATION + STICKY BARS + TUTORIAL FIX (WEB) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

V2.4a — Deferred correction application + Back button
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- Restock workflow (2-step: Check Shelves → Record Purchases) had no Back
  button between steps. Users who advanced to Step 2 and needed to correct
  something in Step 1 had to cancel and redo the entire process.
- Step 2 tutorial highlight was also broken — advancing from step 5 (Continue
  button) to step 6 (Step 2 section) didn't trigger the restock step
  transition, so the Step 2 UI wasn't rendered yet.

SOLUTION:
- Deferred correction application: corrections are NO LONGER applied to
  product quantities when clicking "Continue to Purchases". Instead,
  corrections are stored in restockTemp.corrections and only applied to
  products when "Done" is clicked in completeRestock().
- This allows a Back button on Step 2 to safely return to Step 1 without
  data inconsistency (corrections haven't been committed yet).
- Added "← Back to Check Shelves" button on Step 2 that calls showStep(1).
- cancelRestock() no longer needs to revert corrections (they were never
  applied).

WHAT CHANGED (code):
- restock.js: continueToPurchases() — removed saveProductsOnly() call;
  corrections are now only applied in completeRestock() before recording
  purchases.
- restock.js: completeRestock() — applies corrections to products then
  records purchases (was: corrections already applied, just recorded
  purchases).
- restock.js: cancelRestock() — simplified; no longer reverts corrections
  (they were never applied to products).
- restock.js: added goToStep1() function — navigates back to Step 1
  without any data changes.
- restock.html: added Back button on Step 2 (#restockBackBtn) with
  onclick="window.restockGoToStep1()"

FILES AFFECTED:
  restock.js, restock.html

V2.4b — Tutorial step transition fix
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- When the restock tutorial advances from step 5 (Continue button) to step
  6 (Step 2 section), the restock page was still showing Step 1.
- The Step 2 highlight targets (restockStep2Section, restockPurchaseList,
  restockDoneBtn) didn't exist in the DOM, so the tutorial overlay had
  nothing to highlight.

SOLUTION:
- Added special-case logic in the tutorial advance handler (app.js) so that
  when advancing from step 5 to step 6 in the restock tutorial, the restock
  step transition is triggered (showStep(2)) before the overlay renders.
- Similarly, going back from step 6 to step 5 triggers showStep(1).

WHAT CHANGED (code):
- app.js: in the tutorial advance logic (renderTutorialStep), added check:
  if current page tutorial is 'restock' and next step index >= 5,
  trigger window.restockShowStep(2). If prev step index <= 4,
  trigger window.restockShowStep(1).
- restock.js: exposed showStep() as window.restockShowStep() for cross-file
  access from app.js.

FILES AFFECTED:
  app.js, restock.js

V2.4c — Back-to-top button on restock Step 1
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Floating circle button appears at bottom-right of restock page Step 1
- Button appears when user scrolls past the product list
- Tap to smooth-scroll back to top
- Button disappears on Step 2

WHAT CHANGED (code):
- restock.html: added #restockBackToTopBtn (absolute position, bottom-right)
  inside #restockStep1
- restock.js: added scroll listener on #appContent — toggles button visibility
  based on restockTemp.step and scroll position

FILES AFFECTED:
  restock.html, restock.js

V2.4d — Sticky bottom action bars (both steps)
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- On Step 1, the "Continue to Purchases →" button was at the bottom of the
  product list, requiring users to scroll all the way down to proceed.
- On Step 2, the "Done ✓" button had the same issue.

SOLUTION:
- Both steps now have a sticky bottom action bar that stays fixed at the
  bottom of the screen while the content scrolls above it.

Step 1 sticky bar: [Cancel] [Continue to Purchases →]
Step 2 sticky bar: [← Back] [Done ✓]

WHAT CHANGED (code):
- restock.html: Step 1 — removed inline Continue/Cancel buttons from
  #restockProductList; added #restockStickyBar (outside #restockStep1)
  with Cancel (secondary) + Continue (primary) in flex row. Added
  padding-bottom: 80px to #restockProductList for clearance.
- restock.html: Step 2 — removed inline Done/Cancel buttons from
  #restockPurchaseTotal; added #restockStickyBar2 with Back (secondary)
  + Done (primary) in flex row. Added margin-bottom: 80px to total row.
- style.css: added .restock-sticky-bar class — position:absolute;
  bottom:0; flex row; border-top + shadow; z-index:15.
- restock.js: showStep() — shows/hides #restockStickyBar (step 1) and
  #restockStickyBar2 (step 2); added dom.restockStickyBar and
  dom.restockStickyBar2 to DOM cache.

FILES AFFECTED:
  restock.html, restock.js, style.css

V2.4e — Restock tutorial text updated
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- restockTutorial6: Added "Use the Back button to return to Step 1 if needed."
- restockTutorial8: Changed "Find the reminder on Morning page." →
  "You can also go Back to make changes before finishing."

WHAT CHANGED (code):
- app.js: updated i18n EN + FIL strings for restockTutorial6 and
  restockTutorial8

FILES AFFECTED:
  app.js

ALL FILES AFFECTED (V2.4):
  restock.html, restock.js, style.css, app.js

VERIFICATION:
- node --check app.js — clean.
- node --check restock.js — clean.

================================================================================
V2.5 — SETTINGS "MORE" SECTION FILIPINO TRANSLATION FIX (WEB) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED

PROBLEM:
- Settings page "More" section title was translated as "Higit Pa" in Filipino.
- "Higit" primarily means "greater than / exceeding" and is used for
  comparisons and quantities. As a section heading for grouping secondary
  features (Reports + Help), it sounds unnatural and vague.

SOLUTION:
- Changed FIL translation from "Higit Pa" to "Iba Pa" ("Others" /
  "Additional"). "Iba" = other/different, "Pa" = also/still/additional.
  This is the standard Filipino UI pattern for catch-all grouping sections.

WHAT CHANGED (code):
- app.js: moreSection FIL string: 'Higit Pa' → 'Iba Pa'

FILES AFFECTED:
  app.js

VERIFICATION:
- node --check app.js — clean.

================================================================================
V2.6 — SETTINGS PAGE REORGANIZATION (WEB) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- Settings page was a flat, ungrouped list of10 sections with no logical
  hierarchy. Tutorials and More (Reports/Help) were navigation shortcuts
  mixed in with actual settings. Each inventory default (Markup, Threshold,
  Credit Limit) had its own section title.

SOLUTION:
- Reorganized into 5 logical groups:
    Store Profile: Store Name + Owner Name + Language
    Display: Text Size
    Inventory Defaults: Markup + Threshold + Credit Limit (one group)
    Support: Help + Reports
    Data: Export + Import (disabled, mobile-only) + Reset (red, destructive)
- Removed Tutorials section (belongs in Help page)
- Removed More section (Reports/Help are pages, not settings)
- Added Import Data button (disabled, labeled "Mobile only")
- Reset button styled as btn-danger (red)

WHAT CHANGED (code):
- setting.html: restructured into5 grouped sections with comments;
  removed Tutorials and More; added Support group (Help, Reports);
  added Import Data (disabled); Reset styled as btn-danger
- style.css: added .settings-section-item (label + spacing);
  added .btn-danger (red destructive button)
- app.js: added 6 new i18n keys (EN + FIL): settingsSectionProfile,
  settingsSectionDisplay, settingsSectionDefaults, settingsSectionSupport,
  settingsSectionData, importDataBtn

FILES AFFECTED:
  setting.html, style.css, app.js

VERIFICATION:
- node --check app.js — clean.

================================================================================
V2.7 — CLOSING PAGE COLLAPSIBLE SECTIONS + LOW STOCK FILTER (WEB) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

V2.7a — Collapsible low stock, sold items, debts sections
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- Closing page sections (Mga Nabenta, Mga Kulang sa Stock, Utang Hindi Pa
  Bayad) were always expanded, requiring scrolling through all content.

SOLUTION:
- Made all3 sections collapsible with click-to-toggle.
- Section title is clickable with up/down arrow indicator.
- Content collapses with smooth max-height + opacity animation.
- All3 default to expanded.

WHAT CHANGED (code):
- closing.html: added closing-collapsible class + arrow SVG to section
  titles; added collapsing class to content divs for sold, low stock,
  and debts sections
- style.css: added .closing-collapsible (clickable flex row),
  .collapse-arrow (rotation transition), .collapsing/.collapsed
  (max-height + opacity animation)
- app.js: added toggleCollapse() function with state tracking,
  arrow rotation, and smooth expand/collapse

V2.7b — Low stock filter synced to mobile
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- Web showed ALL low/out-of-stock items (no limit) in insertion order.
- Mobile showed top5 only, LOW items first (sorted by qty), then
  OUT_OF_STOCK (sorted by name).
- Web showed "Out of stock" for qty=0; mobile showed "X left" for all.

SOLUTION:
- Web low stock filter now matches mobile:
    LOW items first (sorted by quantity ascending)
    OUT_OF_STOCK items next (sorted by name)
    Limited to top5 items
    All items show "X left" (not "Out of stock")

WHAT CHANGED (code):
- app.js: closing page low stock rendering now uses two separate
  filters (LOW + OUT_OF_STOCK) with independent sorting, concatenated
  and sliced to5; display text changed from t('noStock') to
  p.quantity + ' left' for all items

FILES AFFECTED (V2.7):
  closing.html, style.css, app.js

VERIFICATION:
- node --check app.js — clean.

================================================================================
V2.8 — MORNING CARDS REDIRECT + RESTOCK DATE FIX + TUTORIAL REPLAY HINT (WEB) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

V2.8a — Morning cards clickable redirects
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- The first two morning cards (Stock warnings + Debt summary) were static
  divs with no onclick handlers. Tapping them did nothing.
- Mobile app already had Card(onClick) on both cards.

SOLUTION:
- Added onclick handlers + cursor:pointer to both morning cards:
    Stock warnings card → navigates to inventory.html
    Debt summary card → navigates to debts.html

WHAT CHANGED (code):
- morning.html: added onclick + cursor:pointer to stock warning card
  (window.location.href='inventory.html')
- morning.html: added onclick + cursor:pointer to debt summary card
  (window.location.href='debts.html')

FILES AFFECTED:
  morning.html

V2.8b — Corrupted restock date auto-clear
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- getDaysSinceLastRestock() parsed an invalid/old localStorage date
  without validation, producing "20685 days" (~56 years) on the morning
  page restock reminder card.

SOLUTION:
- Added 3 guards in getDaysSinceLastRestock():
    isNaN(then.getTime()) → invalid date → clear + return -1
    diff > 365 → unreasonably old → clear + return -1
    catch(e) → parse error → clear + return -1
- Corrupted sss_v3_lastRestockDate auto-cleared on next page load.

WHAT CHANGED (code):
- app.js: getDaysSinceLastRestock() — added date validation, >365 guard,
  and try/catch with localStorage.removeItem fallback

FILES AFFECTED:
  app.js

V2.8c — Tutorial replay hint (? button highlight)
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- The header ? button (headerTutorialBtn) was never highlighted in any
  tutorial. Users had no in-tutorial cue that they could replay tutorials.

SOLUTION:
- Added a final step to every tutorial (16 total: 14 page-specific +
  main) that highlights the ? button with text:
  "You can always tap the ? button to replay this tutorial."

WHAT CHANGED (code):
- app.js: added i18n key tutorialReplayHint (EN + FIL)
- app.js: added final step (highlight: '#headerTutorialBtn') to all 16
  tutorial step arrays

FILES AFFECTED:
  app.js

ALL FILES AFFECTED (V2.8):
  morning.html, app.js

VERIFICATION:
- node --check app.js — clean.

================================================================================
V2.9 — BFCACHE STALE DATA FIX (WEB) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- Chrome's back/forward cache (bfcache) restores frozen page DOM with stale
  data when navigating back. For example: deduct stock on product_detail.html,
  press Back → inventory.html shows old quantity because DOMContentLoaded
  does not fire on bfcache restore, so loadState() + render never re-run.
- Forward link navigations (e.g. Inventory → Debts → Inventory) always do
  a full page load, which is why the round-trip shows the fresh value.
- Only add_product.html had a pageshow listener (added earlier for its own
  bfcache issue). All other state-rendering pages were vulnerable.

SOLUTION:
- Added pageshow event listeners to all 7 state-rendering pages. When
  ev.persisted is true (page restored from bfcache), re-runs loadState()
  + applyTranslations() + the page's render function to refresh the DOM.

Pages patched:
  inventory.html   — loadState + loadInventoryCatFilter + renderInventoryCatFilters +
                     renderManageInventory + applyTranslations
  debts.html       — loadState + applyTranslations + renderManageDebts
  product_detail   — loadState + applyTranslations + renderProductDetail
  debtor_detail    — loadState + applyTranslations + renderDebtorDetail
  record_payment   — loadState + applyTranslations + renderRecordPayment
  reports.html     — loadState + applyTranslations + loadReportPeriod + renderReports
  expenses.html    — loadState + applyTranslations + renderExpenses
  (add_product — already had pageshow handler, unchanged)

WHAT CHANGED (code):
- app.js: added window.addEventListener('pageshow', ...) in each page's
  init branch, mirroring the existing add_product pattern

FILES AFFECTED:
  app.js

VERIFICATION:
- node --check app.js — clean.

================================================================================
