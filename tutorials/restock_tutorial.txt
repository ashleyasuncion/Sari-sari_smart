SARI-SARI SMART — RESTOCK DAY TUTORIAL (8 STEPS)
=================================================
Config: app.js -> tutorials.restock
Page: restock
Launch: header tutorial button on restock.html (startTutorial('restock', true)) or Settings selector

NOTE ON TRANSLATION: The `restockTutorial1..8` keys exist ONLY in the English
(en) dictionary of app.js — there is NO Filipino (fil) translation. When the
app language is Filipino, the `t()` helper falls back to the English string
(strings[lang][key] || strings.en[key] || key). All 8 steps therefore show the
English text below in both languages.

SHARED STEP FIELDS
- Highlight appearance: fixed frame, z-index 151, white 2px + green 4px rings, RAF-tracked
- Navigation: Next advances; Skip visible (replay); last step Next finishes in place
- Box position rule: target in top 40% -> box bottom; else top; no highlight -> centered

----------------------------------------------------------------------
STEP 1 — Intro
  Text EN : "This is the Restock Day page - a guided 2-step workflow to update inventory."
  Text FIL: (none — falls back to English, see note above)
  Target element : none
  Highlight      : none
  Box position   : centered
  Interaction    : read text, tap Next
----------------------------------------------------------------------
STEP 2 — Step 1: shelf check
  Text EN : "Step 1: Check your shelves. Update the actual count for each product."
  Text FIL: (none — falls back to English, see note above)
  Target element : #restockStep1
  Highlight      : Step 1 section header (top) -> box expected at bottom
  Interaction    : look at step 1, tap Next
----------------------------------------------------------------------
STEP 3 — Search
  Text EN : "Enter the real count you see on your shelf next to what the app thinks."
  Text FIL: (none — falls back to English, see note above)
  Target element : #restockSearch
  Highlight      : Search box -> box per runtime rule
  Interaction    : look at search, tap Next
----------------------------------------------------------------------
STEP 4 — Product list
  Text EN : "Green check = count matches. Red = unrecorded sales were found."
  Text FIL: (none — falls back to English, see note above)
  Target element : #restockProductList
  Highlight      : Product list with status icons -> box per runtime rule
  Interaction    : look at list, tap Next
----------------------------------------------------------------------
STEP 5 — Continue to Purchases
  Text EN : "After checking, tap Continue to Purchases to record what you bought."
  Text FIL: (none — falls back to English, see note above)
  Target element : #restockContinueBtn
  Highlight      : Continue button -> box per runtime rule
  Interaction    : see the button, tap Next
----------------------------------------------------------------------
STEP 6 — Step 2: purchases
  Text EN : "Step 2: Search for a product, enter cost per unit and quantity, tap Add Item."
  Text FIL: (none — falls back to English, see note above)
  Target element : #restockPurchaseProduct
  Highlight      : Step 2 product entry -> box per runtime rule
  Interaction    : look at entry, tap Next
----------------------------------------------------------------------
STEP 7 — Purchase list
  Text EN : "Review your purchases. Total cost is shown at the bottom."
  Text FIL: (none — falls back to English, see note above)
  Target element : #restockPurchaseList
  Highlight      : Purchase list -> box per runtime rule
  Interaction    : look at list, tap Next
----------------------------------------------------------------------
STEP 8 — Done
  Text EN : "Tap Done to save. Your inventory is updated. Find the reminder on Morning page."
  Text FIL: (none — falls back to English, see note above)
  Target element : #restockDoneBtn
  Highlight      : Done button (bottom) -> box expected at top
  Interaction    : see the button, tap Next (finishes tutorial in place)
